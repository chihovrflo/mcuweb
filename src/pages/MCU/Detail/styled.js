/* eslint-disable import/prefer-default-export */
import styled from 'styled-components';
import {
  TextField,
  Container,
} from '@material-ui/core';

export const DetailInput = styled(TextField)``;
export const DetailRoot = styled(Container)``;
